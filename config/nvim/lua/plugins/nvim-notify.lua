---@type LazySpec
-- TODO(serhalp) Check if this is best configured at the AstroUI level in v4?
return {
  "rcarriga/nvim-notify",
  opts = {
    -- Still necessary?
    -- background_colour = "#000000"
  }
}
